<?php
$koneksi = mysqli_connect("server-name-or-ip", "username", "password", "database-name");

// Check connection
if ($koneksi->connect_error) {
    die("Connection failed: " . $koneksi->connect_error);
}
?>